# Findings register

One writer, namespaced ids, closed **only by evidence** — never by a reply.
A row a `grep` cannot find is a finding that silently ceased to exist, so the
table's shape is fixed and parsed by the gates that read it.

Severity: `S1` blocker · `S2` must fix before the milestone closes · `S3` should
fix · `S4` note.

**Closure gate:** no `S1` or `S2` may cross a milestone boundary still open.

| id | date | severity | story | owner | finding | status | closing evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| F-001 | 2026-09-20 | S2 | M2-S1 | data scientist | `deskew="log"` on a column with values ≤ −1 (`longitude`) returned a frame of NaNs with no error; the only signal was a RuntimeWarning from inside sklearn's extmath naming neither the column nor the step | **closed** | `_checked_log1p` raises `DomainError` naming the column and the cure; `test_log_on_an_out_of_domain_column_is_refused_by_name` + `test_the_guard_does_not_fire_on_valid_input` |
| F-002 | 2026-09-20 | S3 | M2-S1 | data scientist | The synthetic fixture drew count columns from a **uniform** distribution, so it reproduced the real file's column names while losing the 3.4–4.9 skew those columns exist to exercise; a de-skew test was asserting a log reduces the skew of a uniform | **closed** | Fixture now draws log-normal (measured synthetic skew 2.9–3.4); `test_log_deskew_actually_reduces_skew` passes and is no longer vacuous |
| F-003 | 2026-09-20 | S2 | M2-S1 | data scientist | Two **M1-S2** tests relied on a particular random draw producing a singleton composite group rather than planting one, so an unrelated fixture change turned one into a test that asserted nothing and broke the other; one of them also carried a `pytest.skip` that excused the same problem | **closed** | Both now use `_frame_with_singleton_island`, which plants the real file's arrangement; the skip is gone and the singleton is asserted |
| F-004 | 2026-09-20 | S1 | M2-S4 | data scientist | The engineered ratios are unbounded and the dataset contains institutional block groups (one with 6 households and 7 460 people, `population_per_household` = 1 243 vs a median of 2.8). Standard-scaled, such a row is a z-score in the hundreds; on a 5 000-row subsample one CV fold returned **RMSE 1 805 055** beside neighbours around 65 000 | **closed** | `QuantileClipper` winsorises at train-learned quantiles inside the fold; folds became `[61694, 69169, 63222, 65349, 72352]` and full-data Ridge improved 65 838 -> 63 811. Regression test: `test_an_institutional_block_group_does_not_blow_up_predictions` |
| F-005 | 2026-09-20 | S2 | M3-S1 | data scientist | The synthetic fixture's target was a uniform random draw, so it reproduced the dataset's shape but not its **learnability**. No model can beat a median predictor on noise, so the first model-level test (`test_a_real_model_beats_the_dummy`) failed against correct code | **closed** | Target is now a crude but real function of income, latitude and crowding plus noise; corr(target, median_income) = 0.91, and the cap / rare level / null structure is preserved. Tests still assert relationships, never coefficients |
| F-006 | 2026-09-20 | S1 | M3-S2 | data scientist | A search that replaces a pipeline step installs an estimator that was never `set_output`-configured, so the branch returns a bare ndarray and loses its index. Every draw raised `ValueError: Concatenating DataFrames ... inconsistent number of samples` from inside `ColumnTransformer`, naming neither the branch nor the step. **The existing test missed it twice**: it tried only `values[0]`, and it fitted on `X.iloc[:200]`, whose `RangeIndex` accidentally aligned with the ndarray's default index | **closed** | `numeric._pandas` configures every factory output at construction; the test now fits **every** value on a **shuffled** index and asserts the index is not a RangeIndex; `test_a_factory_result_is_safe_to_drop_into_a_pipeline` pins the root cause |
| F-007 | 2026-09-20 | S1 | M4-S2 | ML engineer | `uv build --out-dir kaggle/src_dataset` writes a `.gitignore` containing `*` into its output directory, so git silently ignored that whole tracked folder and `dataset-metadata.json` was never committed. It existed on the machine that wrote it, the layout gate said "all declared", and CI failed on a runner that had only what was committed | **closed** in M4-S3 | The wheel now builds into `artifacts/wheel/` (already ignored) and is copied across; the stray ignore file lands somewhere harmless |
| F-008 | 2026-09-20 | S1 | M4-S2 | ML engineer | The layout gate enforced only *tracked implies declared*. A **declared path that was never committed passed silently** - which is what let F-007 reach main | **closed** in M4-S3 | `check_layout.missing()` adds the other direction with its own message (`DECLARED BUT NOT TRACKED`); red-teamed by untracking both metadata files, and it immediately caught a second real gap (`docs/model-card.md` declared but unwritten) |
| F-009 | 2026-09-20 | S2 | M4-S2 | ML engineer | **Process defect, mine.** PR #13 was merged while `test (3.11)` and `test (3.12)` were FAILING. The wait-loop polled until the checks were *complete* and then merged without inspecting their **conclusion**, so "merge only when green" was enforced by nothing | **closed** in M4-S3 | Merge command now asserts every check's conclusion is `SUCCESS` before calling `gh pr merge`. The underlying defect (F-007) was fixed in this story under "Also fixes", per the protocol's rule for a defect found after a merge |
| F-010 | 2026-09-21 | S1 | M4-S3.1 | ML engineer | **Broken production.** The published kernel errored on its first data cell: `config.resolve_csv` hardcoded `/kaggle/input/california-housing-prices/housing.csv`, which is not where the file was, and the refusal listed only the paths it wanted - nothing about what was actually mounted | **closed** | The mount is now SEARCHED by filename (`_kaggle_candidates`), one and two levels deep, and the refusal prints the contents of `/kaggle/input`. Four tests, including one using a slug this project has never heard of |
| F-011 | 2026-09-21 | S1 | M4-S3.2 | ML engineer | The first mount fix globbed `*/` and `*/*/` and still missed: the real mount is `/kaggle/input/datasets/camnugent/california-housing-prices/housing.csv`, deeper than either pattern. Guessing the depth is the same mistake as guessing the slug | **closed** | `rglob`, which cannot miss at any depth; the refusal now lists every CSV present with full paths |
| F-012 | 2026-09-21 | S1 | M4-S3.3 | ML engineer | **Broken production, and a systemic gap.** `KBinsDiscretizer(quantile_method=...)` needs scikit-learn **1.7**, but `pyproject` declares `>=1.5`. Kaggle's image is below 1.7, so pip was already satisfied and installed nothing newer; the published kernel died with `TypeError: unexpected keyword argument 'quantile_method'`. **CI had only ever resolved the newest versions in range, so the declared floor was never tested** | **closed** | The kwarg is feature-detected from the signature, not a version comparison. And a `lowest-versions` CI job resolves `--resolution lowest-direct` (sklearn 1.5.0, pandas 2.2.0, numpy 1.26.0, lgbm 4.0.0, xgb 2.0.0) and runs the suite against it - verified to reject the old code and accept the new |
